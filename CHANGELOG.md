# Changelog

## [Unreleased]

### Added
- **Observability standard, Wave 0 — the shared three-pillar contract** (spec `docs/superpowers/specs/2026-08-18-metrics-integration-design.md` §3, §3b, §3c). All 14 Go service repos import these names in Wave 1.
  - `pkg/metrics`: `MountOn(e *echo.Echo)` and `StartServer(addr) (stop func(context.Context) error)` for `/metrics` exposition on the shared `Registry`; `HTTPRequestDuration` (`http_request_duration_seconds{method,route,status_class}`) and `GRPCServerHandling` (`grpc_server_handling_seconds{grpc_service,grpc_method,grpc_code}`) registered at package init; `DefaultBuckets`, `StatusClass(code)`, `RouteUnmatched`, `Path`.
  - `pkg/middleware`: `Metrics()`, `MetricsUnaryServerInterceptor()`, `Tracing()`, `RequestLogger(base, opts...)` + `WithUpstream(fn)`, `HeaderRequestID`. Wire order is `Tracing` → `RequestLogger` → `Metrics`. All skip `/metrics`, `/health`, `/livez`, `/readyz` and clamp an unmatched route to `unmatched`.
  - `pkg/tracing` (new package): `Init(serviceName) (shutdown func(context.Context) error, err error)` — global tracer provider + W3C propagator, OTLP/gRPC exporter. With `OTEL_EXPORTER_OTLP_ENDPOINT` unset it installs a no-op provider and returns a **nil error**, so a service boots identically with no Tempo present. Honours `OTEL_TRACES_SAMPLER` / `OTEL_TRACES_SAMPLER_ARG` (default `parentbased_traceidratio` at 1.0).
  - `pkg/logger`: `IntoContext(ctx, l)` / `FromContext(ctx)` — request-scoped logger keyed on `context.Context` (not `echo.Context`, so repositories, gRPC clients and Kafka consumers can use it). `FromContext` never returns nil.
  - `pkg/envelope`: `HeaderTraceparent` (`"traceparent"`) + `Headers.Traceparent()`. No migration needed — the outbox row already carries `headers JSONB` and the relay copies every entry verbatim.
  - `pkg/events`: `WithGroup(id)` option (Kafka `GroupID` **verbatim**; unset ⇒ `GroupUnknown`) and the six `events_consumer_*` counter families. Each message is handled inside a consumer span continuing the producer's trace as a remote parent; dispatcher log lines gain `event_id`, `topic`, `trace_id`, `span_id`, and the same child logger reaches handlers via `logger.FromContext`.
  - `pkg/outbox`: `Store.PendingStats(ctx)` (single round trip), `Config.MetricsInterval` (`OUTBOX_METRICS_INTERVAL`, default 15s), `DefaultMetricsInterval`, and the six `outbox_*` families. The gauge sampler is a third goroutine under `Start`, stopped by the same `stop()`. Both backlog gauges publish from process start, including on an empty table; `outbox_last_success_timestamp_seconds` is set at relay start and on every error-free poll including zero-row cycles.
  - `PublishProto` injects `traceparent` from the **producing request's** context, so one trace spans request → outbox → relay → consumer. Captured at insert, never at relay.
- `pkg/notifyrender` — **Р56·В-53's second edition on both parts shop-lift texts** (`O-67`, owner ruling of 01.09.2026, option (a)). `parts_shop_sanction_lifted` and `parts_shop_verification_restored` gain a third branch: when the shop's price list is already older than fourteen days at the moment the sanction is lifted, the seller is told what to DO — «Предложения вернутся после загрузки свежего прайса — текущий устарел» — instead of the generic «another cause remains». The vault gives that tail a stated purpose: «иначе продавец ждёт возврата витрины, которого не будет». **No contract change:** the new `remaining_price_stale` optional param is DERIVED in `ExtractParams` from the `repeated string remaining_causes` the wire already carries — the flattening to one joined string happens in this package, and a flat string cannot answer «is price age among them?» because Go's text/template has no substring test in its default function set and the renderer registers none. ⚠ The obvious cheaper form, `{{if eq .remaining_causes "PRICE_STALE"}}`, is REFUSED and a test enforces the refusal: it renders the tail when price age is the only cause and silently falls back when the shop is also paused — passing every single-cause test and failing exactly the multi-cause case the rule exists for. Membership, not exclusivity, is the reading.
- `pkg/notifyrender` — **the parts (Маркетплейс запчастей) catalog entries**: twelve `NOTIFICATION_TYPE_PARTS_*` types gain a `typeKey`, a `BaselineEN` title/body pair, a `requiredParams`/`optionalParams` contract and an `ExtractParams` case. Ten of them have a live producer in sale-service's `PartsNotificationDirectiveBuilder` (the storefront clock's six plus the admin sanction pair and the two shop-level lifts); the remaining two are the price-file importer's pair, which has no producer yet. **This closes a live dead-letter, not a gap:** inbox-service wraps `ExtractParams`' error raw and wires no retry tier, so before these entries every parts directive was dead-lettered onto `notification.events.dlq.inbox` on first delivery — reproduced, and pinned against regression, by `TestPartsDirectivesRenderRatherThanDeadLetter` / `TestPartsProducedTypesAreAllInTheCatalog` in `parts_liveproof_test.go`. **No parts template names the shop**: `shop_name` is `reserved` on all 53 `SendParts*` messages (М-01 makes the shop's name the company's name and the client resolves it from the tenant id), so the copy addresses the recipient's own shop as «your shop». No arm reads `currency` either (`DECISIONS` §30.2 removed it; the «₽» in the baseline is a rendering, not data). Two new helpers: `countOrEmpty` (an optional int32 count must render "" and not "0", or its `{{if}}` guard is always true) and `causesOrEmpty` (Р56·В-54's conditional lift text, where an EMPTY cause list is the good case). `TestOptionalParamsScopedToDelivery` becomes `TestOptionalParamsScopedToDeclaredTypes` — the optional tier is no longer delivery-only, and the whitelist now names all nine parts types that use it.
- `pkg/authz` — `X-Tenant-Memberships` header codec (tenant-unification, spec §4/§12.1). `TenantMembership{TenantID, Role string}`, `ParseTenantMemberships(header) []TenantMembership`, `FormatTenantMemberships(ms) string`, `const HeaderTenantMemberships = "X-Tenant-Memberships"`, and context reader `TenantMembershipsFromContext(c)`. This is the fleet-wide single parser/formatter of the membership header. New wire form is `<tenant_id>=ROLE,...` (e.g. `11111111-...=ADMIN,22222222-...=MANAGER`) — replaces the owner-pair form `TENANT:<uuid>=MANAGER,USER:<uuid>=ADMIN`. `AuthMiddleware` now parses it into `auth.ContextKeyTenantMemberships`.
- `pkg/notifyrender` — typed template rendering for NotificationEnvelope payloads. Templates (TOML, en/ru/kk) embedded; `Render(type, params, locale)` + `ExtractParams(env)` + `RequiredParams(t)` public API. Both notification-service and inbox-service depend on it.
- `pkg/notifyoutbox` — typed producer wrapper for `TOPIC_NOTIFICATION_EVENTS`. `PublishDirective(ctx, pub, tx, env)` validates 10 rules before writing the outbox row. After Phase 4, direct `outbox.Publisher.PublishProto` for that topic is forbidden.
- Tolgee CI workflows (`.tolgeerc.json`, `i18n-{push,pull,consistency}.yml`, `scripts/check-i18n-consistency.sh`) — moves the in-flight Tolgee setup from notification-service to go-common, since templates now live here. Project ID placeholder; inert until ops provisions the Tolgee project.
- Inbox-notif slices 2 & 4 — nine new in-app `NotificationType` catalog entries with TOML (en/ru/kk): `TENANT_REJECTED`, `INVITE_ACCEPTED`, `INVITE_DECLINED`, `ADMIN_TRANSFERRED_NEW`, `ADMIN_TRANSFERRED_OLD` (slice 2 — EMAIL flows now also render IN_APP), and `MEMBER_BLOCKED`, `MEMBER_UNBLOCKED`, `MEMBER_REMOVED`, `TEAM_MEMBER_REMOVED_ADMIN` (slice 4 — team membership lifecycle). EN is real source-of-truth prose; ru/kk seeded with the `TODO_*` placeholder convention pending Tolgee.
- Inbox-notif slice 5 (verbatim free-text) — `notifyrender.IsVerbatim(t)` + `notifyrender.RenderVerbatim(params)` + `ReasonEmptyVerbatim`/`ErrEmptyVerbatimText`. `NOTIFICATION_TYPE_PLATFORM_MESSAGE` carries admin-authored literal title/body and is deliberately NOT in the catalog; consumers branch on `IsVerbatim` before `Render` and copy the text straight into the inbox row.

### Changed
- **(observability, fleet-wide)** `response.JSONError` now writes its diagnostic log line through the **request-scoped logger** (`logger.FromContext(c.Request().Context())`) instead of Echo's `c.Logger().Error(err)`, and derives the level from the HTTP **status class** (5xx→`Error`, 4xx→`Warn`, other→not logged). The line was previously an Echo-gommon `ERROR` carrying `prefix:"echo"` and none of `service`/`request_id`/`trace_id` — unfindable by request id, un-joinable to its trace, and it logged every routine 401/404/422 at `ERROR`, inflating the `{level="error"}` error-rate sparkline (§5.4). It now carries the correlation fields, plus `status`, the free-text `error`, and the typed AppError `reason`. Response body and status are unchanged. Every service calling `response.JSONError` gets this on the next go-common bump (go-common#97).
- **BREAKING** (observability Wave 0) `logger.New(level string)` → `logger.New(level, service string)`. Every repo passes a literal equal to its scrape identity in Wave 1; the returned logger carries `zap.String("service", …)` on every line.
- **BREAKING** (observability Wave 0) the logger's **JSON encoder is now unconditional**. The old `APP_ENV == "production"` gate never selected JSON anywhere — compose and both k8s overlays set `local`/`prod` — so every Go service logged the console encoder in every environment, which Loki's `| json` cannot parse. zap's production sampling is disabled so access lines are never silently dropped.
- `middleware.Logger` is **deprecated** in favour of `middleware.RequestLogger`: it carries no `request_id`, so its lines cannot be correlated across the gateway hop, and it is the `echo.WrapMiddleware` shape whose `responseWriter` has to hand-implement `Flush`/`Unwrap` to keep SSE alive. It stays exported until every service has migrated.
- `outbox.Outbox.Start` now runs **three** goroutines (relay, reaper, metrics sampler) under one `stop()`.
- New dependencies: `go.opentelemetry.io/otel`, `/trace`, `/sdk`, `/exporters/otlp/otlptrace/otlptracegrpc` (v1.43.0).
- **BREAKING** (tenant-unification) `pkg/events` topic registry re-family: the enum value formerly wired to `team.events` now resolves to `tenant.events` (the actor) and the value formerly wired to `tenant.events` resolves to `organisation.events` (the profile). Wire names come from the proto `wire_name` option; the helpers (`TopicName`/`DLQName`/`RetryName`) are unchanged.
- **BREAKING** (tenant-unification) `notifyoutbox.validate()` recipient rule is now exactly-one-of `recipient_user_id` / `recipient_tenant_id` where a recipient is required (IN_APP/PUSH); setting BOTH is always rejected with the new Reason `NOTIFYOUTBOX_AMBIGUOUS_RECIPIENT`. Tenant-addressed envelopes (`recipient_tenant_id` + optional `recipient_role_filter`) are resolved to the live member set at delivery time by each channel-owning consumer. `metadata.locale` is now OPTIONAL on every envelope (consumers resolve per-recipient: envelope hint → user preference → platform default); `NOTIFYOUTBOX_EMPTY_LOCALE` is removed.
- **BREAKING** (tenant-unification) `pkg/notifyrender` baseline/catalog keys re-familied: profile family `tenant_*`→`organisation_*` and `admin_transfer*`→`org_admin_transfer*`; actor family invite keys use the kind taxonomy (`tenant_invite_organisation_*` / `tenant_invite_personal_*`, formerly `team_invite_tenant_*` / `team_invite_user_*`); `team_member_*`→`tenant_member_*`; the `team_name` template param is now `tenant_name`. Earlier entries below that mention `TENANT_REJECTED`/`TEAM_MEMBER_REMOVED_ADMIN`/`team_name` describe the pre-pivot vocabulary superseded by this change.
- `notifyoutbox.validate()` relaxed the recipient_user_id rule from "required unless channels is exactly `[SMS]`" to "required when channels contains IN_APP or PUSH". This unblocks anonymous flows (auth-service login OTPs by SMS or email, team-service phone-keyed invites) without changing the safety contract for in-app/push.
- `notifyrender.ExtractParams` gained cases for the three new auth-service payloads (`SendLoginOtpSms`, `SendLoginOtpEmail`, `SendNewDeviceLogin`). They are NOT in the in-app catalog — they go through notification-service's legacy email/SMS template path.
- `notifyrender.ExtractParams` `SendAdminTransferredOld`/`SendAdminTransferredNew` cases now pull the new `team_name` field (was an empty map) so the in-app body names the team instead of rendering static text. Slice 4 adds the four member-lifecycle payload cases plus the slice-5 `SendPlatformMessage` verbatim case.
- `notifyoutbox.validateParams` gained a verbatim escape hatch: `PLATFORM_MESSAGE` skips the catalog/required-param contract and is validated only for non-empty title-or-body (`NOTIFYOUTBOX_EMPTY_VERBATIM`).
- **BREAKING** (tenant-unification) JWT membership claim reshaped: `auth.TeamClaim{T,O,R json:"t,o,r"}` → `auth.TenantClaim{ID string json:"id"; R string json:"r"; Kind string json:"kind,omitempty"}`; the owner-type axis is gone (ownership keys on the bare tenant id) and `Kind` is client-display-only (server consumers never branch on it). The `Claims` slice field `Teams []TeamClaim json:"tms,omitempty"` → `Tenants []TenantClaim json:"tns,omitempty"`. `auth.ContextKeyTeamMemberships` (`"team_memberships"`) → `auth.ContextKeyTenantMemberships` (`"tenant_memberships"`).

### Removed
- **BREAKING** (tenant-unification) `pkg/authz` owner-pair apparatus deleted: `OwnerType`/`OwnerUser`/`OwnerTenant`, `Role`/`RoleAdmin`/`RoleManager`/`RoleOperator`, `Membership`/`Memberships`, `Parse`, `CanAccess`, `MembershipsFromContext`, `OwnerTypeFromBool`, and `auth.HeaderTeamMemberships` (`"X-Team-Memberships"`). Per-tenant access decisions now live in each service (against `ParseTenantMemberships` output or via tenant-service); go-common keeps only the header codec and the global-role predicate (`HasMinGlobalRole`).
